import { linkedIn } from "../../assets";
import Icon from "./Icon";

const LinkedInIcon = ({ link }) => (
	<Icon link={link} icon={linkedIn} iconName="linkedIn link" />
);

export default LinkedInIcon;
